package main

import "fmt"

func main() {
	// this two lines work sames
	// := creates a variable - cannot use out of func
	var name1 string = "nico"
	name2 := "nico"

	// = updates a variable
	//name2 = 12
	name2 = "12"

	age := 12
}
